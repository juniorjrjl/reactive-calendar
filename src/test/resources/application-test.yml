spring:
  mongodb:
    embedded:
      version: 4.0.12
  data:
    mongodb:
      port: 27017
      host: localhost
      database: test

retryable:
  max-retries: 1
  min-duration: 1

reactive-calendar:
  mail:
    sender: Reactive Calendar <noreply@reactivecalendar.com>